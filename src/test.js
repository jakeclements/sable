/* ==========================================================================
   Test
   ---
   Used only to test code coverage and functionality of our test systems.
   ========================================================================== */

export function trueOrFalse(param) {
    if (param === true) {
        return true
    }
    return false
}
